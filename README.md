# sessao — skill de Controle de Sessões (v2)

Skill do Claude Code que monta e opera o sistema de controle de sessões (fonte única `PLAN.md` + protocolo planner/executor + rolling-wave). Invoque com `/sessao init|start|handoff|end`.

Templates em `templates/` (espelham o kit `~/workspace/playbook-controle-sessoes/`).
